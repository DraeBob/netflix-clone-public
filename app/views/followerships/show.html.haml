%section.people
  %header
    %h2 People I Follow
  %table.table
    %thead
      %tr
        %th(width="30%")
        %th(width="20%") Videos in Queue
        %th(width="20%") Followers
        %th(width="30%") Unfollow
    %tbody
      - @following_relationships.each do |following_relationship|
        %tr
          %td
            %img(src="http://www.gravatar.com/avatar/#{Digest::MD5.hexdigest('following_relationship.followee.email'.downcase)}?s=40")
            = link_to following_relationship.follower.fullname, user_path(following_relationship.followee)
          %td.extra-padding= following_relationship.followee.queue_videos.count
          %td.extra-padding= following_relationship.followee.followed_relationships.count
          %td.extra-padding
            = link_to following_relationship, method: :delete do
              %i.icon-remove
